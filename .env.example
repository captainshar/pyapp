# Optional. Without it the app falls back to the browser's built-in speech
# recognition, which works but cannot tell speakers apart.
DEEPGRAM_API_KEY=

# Speech model + language passed through to Deepgram.
DEEPGRAM_MODEL=nova-3
DEEPGRAM_LANGUAGE=en-US

# Port for the local server.
PORT=8000
